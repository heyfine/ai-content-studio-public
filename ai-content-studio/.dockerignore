# 依赖与构建产物
node_modules
.next
out
build
coverage

# 环境变量（密钥不进镜像）
.env
.env.*
!.env.example

# 运行时产物与本地文件
public/uploads
*.log
dev-server*.log

# git / 编辑器
.git
.gitignore
next-env.d.ts
*.tsbuildinfo

# 文档（构建上下文只需应用代码；docs 在仓库根，本不在 context 内，此处兜底）
docs
